The frozen inference graph in this folder is the standard pre-trained ssd mobilenet detector.
We use this detector to find traffic lights (which is one of the standard coco image
categories); then apply our own keras classifier to classify as red/yellow/green.

